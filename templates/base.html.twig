<!DOCTYPE html>
<<<<<<< HEAD
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}WatchShop - Montres de Luxe{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        html, body { height: 100%; }
        body {
            display: flex;
            flex-direction: column;
            font-family: 'Playfair Display', serif;
            background: #f5f3ed;
            color: #1a1a2e;
            padding-top: 76px;
            min-height: 100vh;
        }
        main { flex: 1 0 auto; padding: 30px 0 40px; }
        .navbar {
            background: rgba(10,10,26,0.97) !important;
            backdrop-filter: blur(16px);
            padding: 14px 0;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            border-bottom: 1px solid rgba(255,255,255,0.04);
        }
        .navbar-brand {
            font-size: 1.6rem;
            font-weight: 700;
            background: linear-gradient(135deg, #c9a84c, #f0d060);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .navbar-brand i { -webkit-text-fill-color: #c9a84c; margin-right: 8px; }
        .nav-link {
            color: rgba(255,255,255,0.7) !important;
            font-weight: 400;
            padding: 8px 18px !important;
            border-radius: 8px;
            transition: all 0.3s;
            font-size: 0.9rem;
            position: relative;
        }
        .nav-link:hover { color: #c9a84c !important; background: rgba(201,168,76,0.06); }
        .nav-icon {
            position: relative;
            display: inline-block;
        }
        .badge-count {
            position: absolute;
            top: -8px;
            right: -12px;
            font-size: 9px;
            background: #e94560;
            color: white;
            border-radius: 50%;
            padding: 1px 5px;
            min-width: 18px;
            text-align: center;
            font-weight: 600;
        }
        .search-container {
            position: relative;
            max-width: 380px;
            width: 100%;
        }
        .search-container .input-group {
            background: rgba(255,255,255,0.05);
            border-radius: 50px;
            overflow: hidden;
            border: 1px solid rgba(255,255,255,0.06);
        }
        .search-container .input-group:focus-within {
            border-color: #c9a84c;
            background: rgba(255,255,255,0.07);
        }
        .search-container input {
            border: none;
            padding: 8px 18px;
            font-size: 0.85rem;
            outline: none;
            flex: 1;
            background: transparent;
            color: white;
        }
        .search-container input::placeholder { color: rgba(255,255,255,0.3); }
        .search-container .search-btn {
            background: transparent;
            border: none;
            padding: 8px 16px;
            color: #c9a84c;
            cursor: pointer;
        }
        .search-results {
            position: absolute;
            top: calc(100% + 6px);
            left: 0;
            right: 0;
            background: #1a1a2e;
            border-radius: 12px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.4);
            border: 1px solid rgba(255,255,255,0.04);
            z-index: 999;
            max-height: 350px;
            overflow-y: auto;
            display: none;
        }
        .search-results .result-item {
            padding: 10px 18px;
            border-bottom: 1px solid rgba(255,255,255,0.03);
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            color: rgba(255,255,255,0.8);
            font-size: 0.9rem;
            transition: background 0.2s;
        }
        .search-results .result-item:hover { background: rgba(201,168,76,0.05); }
        .search-results .result-item .result-price { color: #c9a84c; font-weight: 600; }
        .search-results .no-result { padding: 24px; text-align: center; color: rgba(255,255,255,0.3); }
        .footer {
            flex-shrink: 0;
            background: #0a0a1a;
            color: rgba(255,255,255,0.4);
            padding: 35px 0 20px;
            border-top: 1px solid rgba(255,255,255,0.03);
        }
        .footer h5 { color: white; font-weight: 600; margin-bottom: 14px; font-size: 0.95rem; }
        .footer a { color: rgba(255,255,255,0.4); transition: color 0.3s; text-decoration: none; }
        .footer a:hover { color: #c9a84c; }
        .footer .social a {
            display: inline-block;
            width: 34px;
            height: 34px;
            border-radius: 50%;
            border: 1px solid rgba(255,255,255,0.05);
            text-align: center;
            line-height: 34px;
            margin-right: 5px;
            transition: all 0.3s;
            color: rgba(255,255,255,0.3);
        }
        .footer .social a:hover { background: #c9a84c; border-color: #c9a84c; color: white; }
        .footer hr { border-color: rgba(255,255,255,0.04); margin: 16px 0; }
        #loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #0a0a1a;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: 99999;
            transition: opacity 0.7s ease;
        }
        #loading-screen.hidden { opacity: 0; pointer-events: none; }
        .loading-logo {
            font-size: 3.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, #c9a84c, #f0d060);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 4px;
        }
        .loading-spinner {
            width: 50px;
            height: 50px;
            border: 3px solid rgba(201,168,76,0.1);
            border-top: 3px solid #c9a84c;
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
            margin-top: 24px;
        }
        .loading-bar {
            width: 180px;
            height: 3px;
            background: rgba(255,255,255,0.04);
            border-radius: 2px;
            margin-top: 24px;
            overflow: hidden;
        }
        .loading-bar-inner {
            width: 0%;
            height: 100%;
            background: linear-gradient(90deg, #c9a84c, #f0d060);
            animation: loadBar 1.6s ease forwards;
        }
        .loading-text {
            color: rgba(255,255,255,0.2);
            font-size: 0.7rem;
            margin-top: 16px;
            letter-spacing: 4px;
            text-transform: uppercase;
        }
        @keyframes spin { to { transform: rotate(360deg); } }
        @keyframes loadBar { 0% { width:0%; } 100% { width:100%; } }
        @media (max-width: 768px) {
            .search-container { max-width: 100%; margin: 6px 0; }
            .navbar-brand { font-size: 1.3rem; }
            body { padding-top: 66px; }
        }
        @media (max-width: 480px) {
            .navbar-brand { font-size: 1.1rem; }
            body { padding-top: 60px; }
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <div id="loading-screen">
        <div class="loading-logo">⌚ WatchShop</div>
        <div class="loading-spinner"></div>
        <div class="loading-bar"><div class="loading-bar-inner"></div></div>
        <div class="loading-text">Luxury Timepieces</div>
    </div>

    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}"><i class="fas fa-clock"></i>WatchShop</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_produits') }}">Collection</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_montre_perso') }}">Créations</a></li>
                    {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
                        <li class="nav-item"><a class="nav-link text-warning" href="{{ path('app_admin') }}">Admin</a></li>
                    {% endif %}
                </ul>

                <div class="search-container mx-auto">
                    <div class="input-group">
                        <input type="text" id="searchInput" placeholder="Rechercher..." autocomplete="off">
                        <button class="search-btn" id="searchButton"><i class="fas fa-search"></i></button>
                    </div>
                    <div id="searchResults" class="search-results"></div>
                </div>

                <ul class="navbar-nav ms-3">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_cart') }}">
                            <span class="nav-icon">
                                <i class="fas fa-shopping-bag"></i>
                                <span class="badge-count" id="cartCount">0</span>
                            </span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_wishlist') }}">
                            <span class="nav-icon">
                                <i class="fas fa-heart"></i>
                                <span class="badge-count" id="wishlistCount">0</span>
                            </span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_notifications') }}">
                            <span class="nav-icon">
                                <i class="fas fa-bell"></i>
                                <span class="badge-count" id="notifCount">0</span>
                            </span>
                        </a>
                    </li>
                    {% if app.user %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                                <i class="fas fa-user"></i> {{ app.user.prenom|slice(0,1)|upper }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="{{ path('app_account') }}">Mon compte</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_account_edit') }}">Modifier</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_montre_perso_mes_creations') }}">Mes créations</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_createur_dashboard') }}">Espace créateur</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_vendeur_dashboard') }}">Espace vendeur</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Déconnexion</a></li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Connexion</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Inscription</a></li>
                    {% endif %}
=======
<html>
<head>
    <meta charset="UTF-8">
<<<<<<< HEAD
    <title>{% block title %}Ma Boutique{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">Ma Boutique</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href="/boutique">Boutique</a></li>
                    <li class="nav-item"><a class="nav-link" href="/admin">Admin</a></li>
>>>>>>> ef7cc5654fc803bae3e04cd492ab462c8f40373d
                </ul>
            </div>
        </div>
    </nav>

<<<<<<< HEAD
    <main>
        {% for message in app.flashes('success') %}
            <div class="alert alert-success alert-dismissible fade show m-3">{{ message }}<button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger alert-dismissible fade show m-3">{{ message }}<button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
        {% endfor %}
        {% for message in app.flashes('warning') %}
            <div class="alert alert-warning alert-dismissible fade show m-3">{{ message }}<button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
        {% endfor %}
        {% block body %}{% endblock %}
    </main>

    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-3">
                    <h5><i class="fas fa-clock" style="color:#c9a84c;"></i> WatchShop</h5>
                    <p style="font-size:0.85rem;line-height:1.6;">L'excellence horlogère depuis 2024.</p>
                    <div class="social mt-2">
                        <a href="#"><i class="fab fa-facebook-f"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-twitter"></i></a>
                    </div>
                </div>
                <div class="col-md-4 mb-3">
                    <h5>Liens</h5>
                    <ul class="list-unstyled" style="font-size:0.8rem;line-height:2.2;">
                        <li><a href="{{ path('app_home') }}">Accueil</a></li>
                        <li><a href="{{ path('app_produits') }}">Collection</a></li>
                        <li><a href="{{ path('app_montre_perso') }}">Créations</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-3">
                    <h5>Contact</h5>
                    <ul class="list-unstyled" style="font-size:0.8rem;line-height:2.2;">
                        <li><i class="fas fa-envelope" style="color:#c9a84c;width:18px;"></i> contact@watchshop.com</li>
                        <li><i class="fas fa-phone" style="color:#c9a84c;width:18px;"></i> +33 1 23 45 67 89</li>
                    </ul>
                </div>
            </div>
            <hr>
            <div class="text-center" style="font-size:0.75rem;">
                <p class="mb-0">&copy; 2024 WatchShop</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(function() {
                document.getElementById('loading-screen').classList.add('hidden');
            }, 700);
        });

        const searchInput = document.getElementById('searchInput');
        const searchResults = document.getElementById('searchResults');
        let timeout;
        if (searchInput) {
            searchInput.addEventListener('input', function() {
                clearTimeout(timeout);
                const q = this.value.trim();
                if (q.length < 2) { searchResults.style.display = 'none'; return; }
                timeout = setTimeout(function() {
                    fetch('/api/search?q=' + encodeURIComponent(q))
                        .then(r => r.json())
                        .then(data => {
                            if (data.results && data.results.length > 0) {
                                searchResults.innerHTML = data.results.map(item => `
                                    <div class="result-item" onclick="window.location.href='${item.url}'">
                                        <span>${item.nom}</span>
                                        <span class="result-price">${parseFloat(item.prix).toFixed(2)} €</span>
                                    </div>
                                `).join('');
                                searchResults.style.display = 'block';
                            } else {
                                searchResults.innerHTML = '<div class="no-result">Aucun résultat</div>';
                                searchResults.style.display = 'block';
                            }
                        })
                        .catch(() => { searchResults.style.display = 'none'; });
                }, 250);
            });
            document.addEventListener('click', function(e) {
                if (!e.target.closest('.search-container')) { searchResults.style.display = 'none'; }
            });
            searchInput.addEventListener('keypress', function(e) {
                if (e.key === 'Enter') {
                    const q = this.value.trim();
                    if (q) { window.location.href = '/produits?search=' + encodeURIComponent(q); }
                }
            });
            document.getElementById('searchButton').addEventListener('click', function() {
                const q = searchInput.value.trim();
                if (q) { window.location.href = '/produits?search=' + encodeURIComponent(q); }
            });
        }

        // COUNTERS
        function updateCounts() {
            const base = '/boutique/public/index.php';
            
            fetch(base + '/panier/count')
                .then(r => r.json())
                .then(data => {
                    const el = document.getElementById('cartCount');
                    if (el) el.textContent = data.count || 0;
                })
                .catch(() => {});

            fetch(base + '/wishlist/count')
                .then(r => r.json())
                .then(data => {
                    const el = document.getElementById('wishlistCount');
                    if (el) el.textContent = data.count || 0;
                })
                .catch(() => {});

            fetch(base + '/notifications/count')
                .then(r => r.json())
                .then(data => {
                    const el = document.getElementById('notifCount');
                    if (el) el.textContent = data.count || 0;
                })
                .catch(() => {});
        }

        document.addEventListener('DOMContentLoaded', function() {
            updateCounts();
            setInterval(updateCounts, 5000);
        });
    </script>
    {% block javascripts %}{% endblock %}
=======
    <main class="container">
        {% block body %}{% endblock %}
    </main>
=======
    <title>{% block title %}Boutique{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('app_accueil') }}">Ma Boutique</a>
    <ul class="navbar-nav">
        <li class="nav-item"><a class="nav-link" href="{{ path('app_boutique') }}">Boutique</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('app_panier') }}">Panier</a></li>
    </ul>
</nav>
<div class="container mt-4">{% block body %}{% endblock %}</div>
>>>>>>> 43fbb94 (Initial project Symfony boutique)
>>>>>>> ef7cc5654fc803bae3e04cd492ab462c8f40373d
</body>
</html>
