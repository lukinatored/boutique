{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
<h1>Panier</h1>
<table class="table">
    <thead>
        <tr><th>Montre</th><th>Quantité</th><th>Prix</th><th>Actions</th></tr>
    </thead>
    <tbody>
        {% for item in montres %}
        <tr>
            <td>{{ item.montre.nom }}</td>
            <td>{{ item.quantite }}</td>
            <td>{{ item.montre.prix * item.quantite }} €</td>
            <td><a href="{{
