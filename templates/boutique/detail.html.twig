{% extends 'base.html.twig' %}

{% block title %}{{ montre.nom }}{% endblock %}

{% block body %}
<<<<<<< HEAD
<a href="{{ path('app_boutique') }}" class="btn btn-secondary mb-3">← Retour à la boutique</a>

<div class="row">
    <div class="col-md-6">
        {% if montre.imageUrl %}
            <img src="{{ montre.imageUrl }}" class="img-fluid" alt="{{ montre.nom }}">
        {% endif %}
    </div>
    <div class="col-md-6">
        <h2>{{ montre.nom }}</h2>
        <p><strong>Prix :</strong> {{ montre.prix }} €</p>
        <p><strong>Stock :</strong> {{ montre.stock }}</p>
        <p>{{ montre.description }}</p>
    </div>
</div>
=======
<h1>{{ montre.nom }}</h1>
{% if montre.imageUrl %}
<img src="{{ montre.imageUrl }}" class="img-fluid" alt="{{ montre.nom }}">
{% endif %}
<p><strong>Prix:</strong> {{ montre.prix }} €</p>
<p><strong>Stock:</strong> {{ montre.stock }}</p>
<p><strong>Catégorie:</strong> {{ montre.categorie.nom }}</p>
<p><strong>Marque:</strong> {{ montre.marque.nom }}</p>
<p>{{ montre.description }}</p>
<a href="{{ path('app_boutique') }}" class="btn btn-secondary">Retour à la boutique</a>
<a href="{{ path('app_panier_ajouter', {'id': montre.id}) }}" class="btn btn-success">Ajouter au panier</a>
>>>>>>> 43fbb94 (Initial project Symfony boutique)
{% endblock %}
