{% extends 'base.html.twig' %}

{% block title %}Montre{% endblock %}

{% block body %}
    <h1>Montre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ montre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ montre.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ montre.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ montre.prix }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ montre.stock }}</td>
            </tr>
            <tr>
                <th>ImageUrl</th>
                <td>{{ montre.imageUrl }}</td>
            </tr>
            <tr>
                <th>DateAjout</th>
                <td>{{ montre.dateAjout ? montre.dateAjout|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_montre_index') }}">back to list</a>

    <a href="{{ path('app_montre_edit', {'id': montre.id}) }}">edit</a>

    {{ include('montre/_delete_form.html.twig') }}
{% endblock %}
